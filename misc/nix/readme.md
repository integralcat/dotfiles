# Mental Model

| Output      | Used by          |
| ----------- | ---------------- |
| `devShells` | `nix develop`    |
| `packages`  | `nix build`      |
| `apps`      | `nix run`        |
| `templates` | `nix flake init` |

